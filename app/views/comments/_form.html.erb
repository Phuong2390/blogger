<%= form_for([@post,@post.comments.build]) do |f| %>
  <p>
        <%= f.label :name %><br>
        <%= f.text_field :name %><br/>
  </p>
  <p>
        <%= f.label :content %><br>
        <%= f.text_area :content %><br/>
  </p>
  <p>
        <%= f.submit %>
  </p>
<% end %>
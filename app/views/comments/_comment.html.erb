<h5><%= comment.name %> <%= time_ago_in_words(comment.created_at)%></h5>
<p><%= comment.content %></p>
<p><%= link_to 'Reply comment' %></p>
<% if user_signed_in? %>
      <p><%= link_to 'Delete', [comment.post,comment],method: :delete, data: {confirm: 'Are you sure?'} %>
<% end %>
